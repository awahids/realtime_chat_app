<div class="container">
  <h1>Chatrooms</h1>
  <ul>
    <% @chatrooms.each do |chatroom| %>
      <li><%= link_to chatroom.name, chatroom_path(chatroom), class: 'btn btn-link' %></li>
    <% end %>
  </ul>
</div>
